export const genPrivateKey = (): string => {
  return '';
};

export const genPublicKey = (privateKey: string): string => {
  return '';
};

export const genAddress = (publicKey: string): string => {
  return '';
};
